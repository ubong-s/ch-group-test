import React from 'react';
import { ResultList } from './ResultList';
import { ToReadList } from './ToReadList';

export const Lists = () => {
  return (
    <div>
      <ResultList />
      <ToReadList />
    </div>
  );
};
